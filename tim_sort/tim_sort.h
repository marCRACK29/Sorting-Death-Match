#ifndef TIM_SORT_H
#define TIM_SORT_H
#include <vector>
using namespace std; 

/**
 * @brief Función iterativa para ordenar todo el arreglo. Utiliza algoritmos de ordenamiento 
 * como insertion sort y merge sort. 
 * 
 * @param arr Arreglo a ordenar
 * @param n Tamaño del arreglo 
 */
void timSort(vector<int>& arr, int n);

#endif